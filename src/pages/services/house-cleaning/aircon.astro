---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import Breadcrumb from "../../../components/seo/Breadcrumb.astro";
import CTABanner from "../../../components/sections/CTABanner.astro";
import { COMPANY } from "../../../data/company";
import { SUB_SERVICES } from "../../../data/subservices";

const service = SUB_SERVICES.find((s) => s.slug === "aircon")!;
---

<BaseLayout title={service.title} description={service.metaDescription}>

  <!-- ページヘッダー -->
  <div class="bg-[#0F2952] text-white overflow-hidden">
    <div class="grid grid-cols-1 lg:grid-cols-12 min-h-[360px]">
      <div class="lg:col-span-7 flex flex-col justify-center px-6 md:px-12 lg:px-16 py-16 lg:py-20">
        <Breadcrumb
          items={[
            { name: "事業内容", href: "/services/" },
            { name: "ハウスクリーニング", href: "/services/house-cleaning/" },
            { name: service.title, href: service.href },
          ]}
        />
        <p class="text-xs font-semibold text-brand-accent uppercase tracking-[0.25em] mt-6 mb-3">{service.subtitle}</p>
        <h1 class="text-3xl md:text-5xl font-black mb-4 leading-tight">{service.title}</h1>
        <p class="text-white/70 text-base md:text-lg max-w-xl leading-relaxed">{service.description}</p>
      </div>
      <div class="lg:col-span-5 relative hidden lg:block">
        <img
          src="https://images.unsplash.com/photo-1585771724684-38269d6639fd?w=800&q=80&auto=format&fit=crop"
          alt="エアコンクリーニング作業イメージ"
          class="absolute inset-0 w-full h-full object-cover"
          loading="eager"
          decoding="async"
        />
        <div class="absolute inset-0 bg-gradient-to-r from-[#0F2952] via-[#0F2952]/30 to-transparent"></div>
      </div>
    </div>
  </div>

  <div class="max-w-4xl mx-auto px-4 md:px-8 py-16 md:py-24">

    <!-- こんな症状ありませんか？ -->
    <section class="mb-16">
      <p class="text-xs font-semibold text-gray-400 uppercase tracking-[0.2em] mb-4">Check</p>
      <h2 class="text-2xl md:text-3xl font-black text-brand-dark mb-8">こんな症状、ありませんか？</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        {[
          "エアコンをつけると嫌なにおいがする",
          "吹き出し口に黒い汚れが見える",
          "以前より冷えが悪い・暖まりにくい",
          "電気代が増えた気がする",
          "アレルギーやくしゃみが気になる",
          "最後にクリーニングしたのが2年以上前",
        ].map((item) => (
          <div class="flex items-start gap-3 bg-gray-50 rounded-lg px-5 py-4">
            <span class="text-brand-accent font-bold flex-shrink-0 mt-0.5">✓</span>
            <p class="text-sm text-gray-700">{item}</p>
          </div>
        ))}
      </div>
      <p class="mt-6 text-sm text-gray-500">一つでも当てはまる場合は、エアコンクリーニングをおすすめします。</p>
    </section>

    <!-- 作業内容 -->
    <section class="mb-16">
      <p class="text-xs font-semibold text-gray-400 uppercase tracking-[0.2em] mb-4">Work</p>
      <h2 class="text-2xl md:text-3xl font-black text-brand-dark mb-8">作業内容</h2>
      <ol class="space-y-4">
        {[
          { step: "01", title: "カバー・フィルター取り外し", body: "本体カバー・フィルター・ルーバーを安全に取り外します。" },
          { step: "02", title: "高圧洗浄（熱交換器・ファン）", body: "業務用高圧洗浄機で熱交換器・ファン・ドレンパンを内部まで洗浄します。" },
          { step: "03", title: "除菌・防カビコーティング", body: "洗浄後に除菌処理を行い、カビの再発を抑制します。" },
          { step: "04", title: "乾燥・組み立て・動作確認", body: "完全乾燥後に組み立て、冷暖房の動作確認を行います。" },
        ].map((item) => (
          <div class="flex gap-5 items-start bg-white border border-gray-100 rounded-lg p-5">
            <span class="flex-shrink-0 w-10 h-10 rounded-full bg-[#0F2952] text-white text-sm font-black flex items-center justify-center">{item.step}</span>
            <div>
              <p class="font-bold text-brand-dark mb-1">{item.title}</p>
              <p class="text-sm text-gray-500 leading-relaxed">{item.body}</p>
            </div>
          </div>
        ))}
      </ol>
    </section>

    <!-- 料金目安 -->
    <section class="mb-16">
      <p class="text-xs font-semibold text-gray-400 uppercase tracking-[0.2em] mb-4">Price</p>
      <h2 class="text-2xl md:text-3xl font-black text-brand-dark mb-8">料金目安</h2>
      <div class="border border-gray-100 rounded-lg overflow-hidden">
        <table class="w-full text-sm">
          <thead>
            <tr class="bg-gray-50 border-b border-gray-100">
              <th class="text-left px-6 py-4 font-bold text-brand-dark">種別</th>
              <th class="text-right px-6 py-4 font-bold text-brand-dark">料金目安（税込）</th>
            </tr>
          </thead>
          <tbody>
            {[
              ["壁掛けエアコン（通常タイプ）", "¥12,000〜¥15,000"],
              ["壁掛けエアコン（お掃除機能付き）", "¥18,000〜¥25,000"],
              ["天井埋込型", "¥20,000〜¥30,000"],
            ].map(([type, price], i) => (
              <tr class={`border-b border-gray-100 ${i % 2 === 1 ? "bg-gray-50/50" : ""}`}>
                <td class="px-6 py-4 text-gray-700">{type}</td>
                <td class="px-6 py-4 text-right font-bold text-brand-dark">{price}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
      <p class="mt-3 text-xs text-gray-400">※ 汚れの程度・機種・作業環境により変動します。正確な金額は無料お見積もりにてご確認ください。</p>
    </section>

    <!-- 関連サービス -->
    <section class="mb-16">
      <p class="text-xs font-semibold text-gray-400 uppercase tracking-[0.2em] mb-4">Related</p>
      <h2 class="text-2xl font-black text-brand-dark mb-6">関連サービス</h2>
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
        {[
          { title: "浴室クリーニング", href: "/services/house-cleaning/bathroom/" },
          { title: "キッチン・レンジフード", href: "/services/house-cleaning/kitchen/" },
          { title: "退去・入居時クリーニング", href: "/services/house-cleaning/moving/" },
        ].map((rel) => (
          <a href={rel.href} class="flex items-center justify-between border border-gray-200 rounded-lg px-5 py-4 hover:border-brand-primary hover:text-brand-primary transition-colors text-sm font-medium text-gray-700 cursor-pointer">
            {rel.title}
            <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        ))}
      </div>
    </section>

  </div>
</BaseLayout>

<CTABanner />
